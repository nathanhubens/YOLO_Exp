# Pinned to versions used to produce the results in experiments.jsonl
# (champion config: pose mAP50-95 = 0.4853 at 14.86% param reduction).
torch>=2.9.0
ultralytics==8.3.162
torch-pruning==1.5.3
fasterai==0.3.0
numpy
tqdm
